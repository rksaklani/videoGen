# videoGen — Python dependencies (PyTorch installed separately — see docs/INSTALL.md)
# Install from repo root:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#   pip install -r Backend/requirements.txt
#
# System binary required on PATH: ffmpeg (apt install ffmpeg / brew install ffmpeg / conda install ffmpeg)
# Optional GPU speedup: flash-attn (project falls back to PyTorch SDPA if missing — see Backend/engine/modules/flash_attn_compat.py)

# === AI Engine ===
opencv-python==4.9.0.80
diffusers==0.33.0
transformers==4.44.2
accelerate==1.1.1
pandas==2.0.3
numpy==1.24.4
einops==0.7.0
tqdm==4.66.2
safetensors==0.4.3
decord==0.6.0
librosa==0.11.0
scikit-video==1.1.11
imageio==2.34.0
imageio-ffmpeg==0.5.1
# Used by preprocessor + common deps; reinstall if PIL.ExifTags / IntEnum import fails:
pillow>=10.4.0

# === API Server ===
fastapi==0.115.12
uvicorn==0.34.2
pyyaml>=6.0
python-dotenv>=1.0.0
python-multipart>=0.0.18
slowapi>=0.1.9

# === Utilities ===
loguru==0.7.2
edge-tts>=7.0.0

# === Database ===
motor>=3.0.0
pymongo>=4.0.0

# === Auth ===
python-jose[cryptography]>=3.3.0
passlib>=1.7.0
bcrypt>=4.0.0

# === Billing ===
stripe>=5.0.0
